<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Fyrid - Tu idea vale dinero. Antes de construirla. Marketplace de ideas innovadoras">
    <title>Fyrid - Tu idea vale dinero. Antes de construirla.</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Pixelify+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --Bg-Primary: #ffffff;
            --BgGp-Primary: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
            --BgGp-Secondary: linear-gradient(135deg, #f59e0b 0%, #f97316 100%);
            --Fills-F1: #f9fafb;
            --Fills-F1-hover: #f3f4f6;
            --Labels-Primary: #1f2937;
            --Labels-Secondary: #6b7280;
            --Labels-Tertiary: #9ca3af;
            --Separators-S1: #e5e7eb;
            --MaskBg-Base: rgba(0, 0, 0, 0.5);
            --Bg-Tertiary-hover: rgba(249, 250, 251, 0.8);
            --primary: #6366f1;
            --primary-dark: #4f46e5;
            --secondary: #10b981;
            --accent: #f59e0b;
            --dark: #1f2937;
            --light: #f9fafb;
            --gray: #6b7280;
            --border-radius: 16px;
            --shadow-sm: 0 4px 6px rgba(0, 0, 0, 0.05);
            --shadow-md: 0 10px 25px rgba(0, 0, 0, 0.1);
            --shadow-lg: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
            --transition: all 0.3s ease-in-out;
        }

        @media (prefers-color-scheme: dark) {
            :root {
                --Bg-Primary: #0f172a;
                --Fills-F1: #1e293b;
                --Fills-F1-hover: #334155;
                --Labels-Primary: #f8fafc;
                --Labels-Secondary: #cbd5e1;
                --Labels-Tertiary: #94a3b8;
                --Separators-S1: #334155;
            }
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 10%);
            color: var(--Labels-Primary);
            line-height: 1.6;
            overflow-x: hidden;
            min-height: 100vh;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        header {
            padding: 2rem 0;
            position: sticky;
            top: 0;
            z-index: 100;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            box-shadow: var(--shadow-sm);
            transition: var(--transition);
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            font-weight: 800;
            font-size: 1.5rem;
            color: var(--primary);
            text-decoration: none;
            font-family: 'Pixelify Sans', sans-serif;
        }

        .logo-icon {
            width: 40px;
            height: 40px;
            background: var(--BgGp-Primary);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: bold;
            font-family: 'Pixelify Sans', sans-serif;
            box-shadow: var(--shadow-md);
        }

        .nav-links {
            display: flex;
            gap: 2rem;
        }

        .nav-links a {
            text-decoration: none;
            color: var(--Labels-Secondary);
            font-weight: 500;
            transition: var(--transition);
            font-size: 0.95rem;
        }

        .nav-links a:hover {
            color: var(--primary);
        }

        .cta-button {
            background: var(--BgGp-Primary);
            color: white;
            padding: 0.875rem 1.75rem;
            border-radius: 12px;
            font-weight: 600;
            text-decoration: none;
            transition: var(--transition);
            box-shadow: var(--shadow-md);
            border: none;
            cursor: pointer;
            font-size: 1rem;
        }

        .cta-button:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
        }

        .hero {
            padding: 6rem 0 4rem;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(99, 102, 241, 0.15) 0%, transparent 70%);
            z-index: -1;
        }

        .hero h1 {
            font-size: 3.5rem;
            font-weight: 800;
            margin-bottom: 1.5rem;
            line-height: 1.2;
            font-family: 'Pixelify Sans', sans-serif;
        }

        .hero h1 .highlight {
            background: var(--BgGp-Secondary);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hero p {
            font-size: 1.25rem;
            color: var(--Labels-Secondary);
            margin-bottom: 3rem;
            max-width: 650px;
            margin-left: auto;
            margin-right: auto;
            font-weight: 400;
        }

        .waitlist-form {
            max-width: 550px;
            margin: 0 auto 3rem;
            display: flex;
            gap: 1rem;
        }

        .waitlist-form input {
            flex: 1;
            padding: 1rem 1.5rem;
            border: 2px solid var(--Separators-S1);
            border-radius: 12px;
            font-size: 1rem;
            font-family: inherit;
            transition: var(--transition);
            background: white;
            color: var(--Labels-Primary);
        }

        .waitlist-form input:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.1);
        }

        .waitlist-form button {
            background: var(--BgGp-Primary);
            color: white;
            padding: 1rem 2rem;
            border: none;
            border-radius: 12px;
            font-weight: 600;
            font-size: 1rem;
            cursor: pointer;
            transition: var(--transition);
            box-shadow: var(--shadow-md);
        }

        .waitlist-form button:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
        }

        .social-proof {
            display: flex;
            justify-content: center;
            gap: 2.5rem;
            flex-wrap: wrap;
            margin-top: 2.5rem;
        }

        .proof-item {
            background: white;
            padding: 1.25rem 2rem;
            border-radius: 12px;
            box-shadow: var(--shadow-sm);
            min-width: 180px;
            border: 1px solid var(--Separators-S1);
        }

        .proof-item strong {
            font-size: 1.75rem;
            font-weight: 700;
            color: var(--primary);
            display: block;
            margin-bottom: 0.25rem;
            font-family: 'Pixelify Sans', sans-serif;
        }

        .proof-item span {
            color: var(--Labels-Secondary);
            font-size: 0.875rem;
            font-weight: 500;
        }

        .features {
            padding: 6rem 0;
            background: var(--Bg-Primary);
        }

        .section-title {
            text-align: center;
            margin-bottom: 4rem;
        }

        .section-title h2 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
            color: var(--Labels-Primary);
            font-family: 'Pixelify Sans', sans-serif;
        }

        .section-title p {
            color: var(--Labels-Secondary);
            max-width: 600px;
            margin: 0 auto;
            font-size: 1.1rem;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
            max-width: 1000px;
            margin: 0 auto;
        }

        .feature-card {
            background: var(--Fills-F1);
            padding: 2.5rem;
            border-radius: var(--border-radius);
            text-align: center;
            transition: var(--transition);
            border: 1px solid var(--Separators-S1);
        }

        .feature-card:hover {
            transform: translateY(-8px);
            box-shadow: var(--shadow-md);
        }

        .feature-icon {
            width: 64px;
            height: 64px;
            background: var(--BgGp-Primary);
            border-radius: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            color: white;
            font-size: 1.75rem;
            font-weight: bold;
            box-shadow: var(--shadow-md);
            font-family: 'Pixelify Sans', sans-serif;
        }

        .feature-card h3 {
            font-size: 1.25rem;
            font-weight: 700;
            margin-bottom: 1rem;
            color: var(--Labels-Primary);
        }

        .feature-card p {
            color: var(--Labels-Secondary);
            font-size: 0.95rem;
            line-height: 1.7;
        }

        .demo {
            padding: 6rem 0;
            background: linear-gradient(135deg, #f0f9ff 0%, #fef3c7 100%);
            position: relative;
            overflow: hidden;
        }

        .demo::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" opacity="0.05"><circle cx="20" cy="20" r="2"/><circle cx="50" cy="50" r="3"/><circle cx="80" cy="80" r="2"/></svg>');
            background-size: 40px 40px;
        }

        .steps {
            max-width: 800px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr;
            gap: 3rem;
        }

        .step {
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            position: relative;
            padding: 2rem;
        }

        .step::before {
            content: '';
            position: absolute;
            top: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 4px;
            height: 100%;
            background: var(--BgGp-Primary);
            z-index: -1;
        }

        .step-number {
            width: 52px;
            height: 52px;
            background: var(--BgGp-Primary);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 700;
            font-size: 1.5rem;
            margin-bottom: 1.5rem;
            position: relative;
            z-index: 1;
            box-shadow: var(--shadow-md);
            font-family: 'Pixelify Sans', sans-serif;
        }

        .step h3 {
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
            color: var(--Labels-Primary);
        }

        .step p {
            color: var(--Labels-Secondary);
            max-width: 450px;
            margin: 0 auto;
            font-size: 1.05rem;
            line-height: 1.7;
        }

        .step-image {
            width: 100%;
            max-width: 420px;
            margin: 1.5rem auto 0;
            border-radius: 16px;
            box-shadow: var(--shadow-lg);
            background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
            height: 220px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 600;
            font-size: 1.1rem;
            border: 3px solid white;
        }

        .categories {
            padding: 6rem 0;
            background: var(--Bg-Primary);
        }

        .categories-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            max-width: 1000px;
            margin: 0 auto;
        }

        .category-card {
            background: white;
            padding: 2rem;
            border-radius: var(--border-radius);
            text-align: center;
            transition: var(--transition);
            border: 2px solid transparent;
        }

        .category-card:hover {
            transform: translateY(-5px);
            border-color: var(--primary);
        }

        .category-icon {
            width: 60px;
            height: 60px;
            margin: 0 auto 1.25rem;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            font-weight: bold;
            border-radius: 12px;
        }

        .category-space .category-icon { background: linear-gradient(135deg, #3b82f6 0%, #6366f1 100%); color: white; }
        .category-industrial .category-icon { background: linear-gradient(135deg, #10b981 0%, #16a34a 100%); color: white; }
        .category-domestico .category-icon { background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%); color: white; }
        .category-primitive .category-icon { background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%); color: white; }

        .category-card h3 {
            font-size: 1.35rem;
            font-weight: 700;
            margin-bottom: 0.75rem;
            color: var(--Labels-Primary);
        }

        .category-card p {
            color: var(--Labels-Secondary);
            font-size: 0.95rem;
            line-height: 1.6;
        }

        .testimonials {
            padding: 6rem 0;
            background: linear-gradient(135deg, #8b5cf6 0%, #ec4899 100%);
            color: white;
        }

        .testimonial-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            max-width: 900px;
            margin: 0 auto;
        }

        .testimonial-card {
            background: rgba(255, 255, 255, 0.95);
            padding: 2rem;
            border-radius: var(--border-radius);
            position: relative;
            color: var(--Labels-Primary);
        }

        .testimonial-card::before {
            content: '"';
            position: absolute;
            top: 1rem;
            left: 1rem;
            font-size: 4rem;
            color: var(--primary);
            opacity: 0.2;
            font-family: serif;
        }

        .testimonial-text {
            margin-bottom: 1.5rem;
            position: relative;
            z-index: 1;
            font-style: italic;
            line-height: 1.7;
        }

        .testimonial-author {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .author-avatar {
            width: 52px;
            height: 52px;
            border-radius: 50%;
            background: var(--BgGp-Primary);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: bold;
            font-size: 1.1rem;
            font-family: 'Pixelify Sans', sans-serif;
        }

        .author-info h4 {
            font-weight: 600;
            margin-bottom: 0.25rem;
            color: var(--Labels-Primary);
        }

        .author-info p {
            color: var(--Labels-Secondary);
            font-size: 0.875rem;
        }

        .cta-section {
            padding: 6rem 0;
            background: var(--BgGp-Primary);
            color: white;
            text-align: center;
        }

        .cta-section h2 {
            font-size: 2.75rem;
            font-weight: 700;
            margin-bottom: 1rem;
            font-family: 'Pixelify Sans', sans-serif;
        }

        .cta-section p {
            font-size: 1.125rem;
            margin-bottom: 2.5rem;
            max-width: 650px;
            margin-left: auto;
            margin-right: auto;
            opacity: 0.95;
            line-height: 1.7;
        }

        .cta-form {
            max-width: 550px;
            margin: 0 auto;
            display: flex;
            gap: 1rem;
        }

        .cta-form input {
            flex: 1;
            padding: 1rem 1.5rem;
            border: none;
            border-radius: 12px;
            font-size: 1rem;
            font-family: inherit;
            background: rgba(255, 255, 255, 0.9);
            color: var(--Labels-Primary);
        }

        .cta-form input:focus {
            outline: none;
            box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.3);
        }

        .cta-form button {
            background: white;
            color: var(--primary);
            padding: 1rem 2rem;
            border: none;
            border-radius: 12px;
            font-weight: 600;
            font-size: 1rem;
            cursor: pointer;
            transition: var(--transition);
        }

        .cta-form button:hover {
            transform: translateY(-2px);
        }

        .cta-benefits {
            display: flex;
            justify-content: center;
            gap: 2rem;
            margin-top: 2.5rem;
            flex-wrap: wrap;
        }

        .cta-benefit {
            background: rgba(255, 255, 255, 0.15);
            padding: 1.25rem 2rem;
            border-radius: 12px;
            min-width: 160px;
            backdrop-filter: blur(10px);
        }

        .cta-benefit strong {
            display: block;
            font-size: 1.35rem;
            margin-bottom: 0.25rem;
            font-family: 'Pixelify Sans', sans-serif;
        }

        .cta-benefit span {
            font-size: 0.95rem;
            opacity: 0.9;
        }

        footer {
            background: var(--Labels-Primary);
            color: white;
            padding: 4rem 0 2rem;
        }

        .footer-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 3rem;
            max-width: 1000px;
            margin: 0 auto;
        }

        .footer-column h3 {
            font-size: 1.125rem;
            font-weight: 600;
            margin-bottom: 1.5rem;
            color: #f3f4f6;
        }

        .footer-links {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 0.875rem;
        }

        .footer-links a {
            color: #cbd5e1;
            text-decoration: none;
            transition: var(--transition);
            font-size: 0.95rem;
        }

        .footer-links a:hover {
            color: white;
        }

        .copyright {
            text-align: center;
            padding-top: 3rem;
            color: #9ca3af;
            font-size: 0.875rem;
            border-top: 1px solid #374151;
            margin-top: 3rem;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .animate-fade-in {
            animation: fadeIn 0.6s ease-out forwards;
        }

        .animate-delay-1 { animation-delay: 0.1s; }
        .animate-delay-2 { animation-delay: 0.2s; }
        .animate-delay-3 { animation-delay: 0.3s; }
        .animate-delay-4 { animation-delay: 0.4s; }

        @media (max-width: 768px) {
            .header-content {
                flex-direction: column;
                gap: 1.5rem;
            }

            .nav-links {
                flex-wrap: wrap;
                justify-content: center;
            }

            .hero h1 {
                font-size: 2.5rem;
            }

            .hero p {
                font-size: 1rem;
            }

            .waitlist-form {
                flex-direction: column;
            }

            .social-proof {
                flex-direction: column;
                gap: 1rem;
            }

            .step::before {
                display: none;
            }

            .cta-benefits {
                flex-direction: column;
            }

            .section-title h2 {
                font-size: 2rem;
            }

            .section-title p {
                font-size: 1rem;
            }
        }

        @media (max-width: 480px) {
            .hero h1 {
                font-size: 2rem;
            }

            .hero p {
                font-size: 0.95rem;
            }

            .waitlist-form input,
            .waitlist-form button {
                padding: 0.875rem;
                font-size: 0.95rem;
            }

            .proof-item {
                padding: 1rem;
                min-width: 150px;
            }

            .proof-item strong {
                font-size: 1.5rem;
            }

            .section-title h2 {
                font-size: 1.75rem;
            }

            .step h3 {
                font-size: 1.25rem;
            }

            .step p {
                font-size: 0.95rem;
            }

            .step-image {
                height: 180px;
                font-size: 0.95rem;
            }
        }
    </style>
</head>
<body>
    <header>
        <div class="container header-content">
            <a href="#" class="logo">
                <div class="logo-icon">F</div>
                <span>Fyrid</span>
            </a>
            <nav class="nav-links">
                <a href="#features">Características</a>
                <a href="#demo">Cómo funciona</a>
                <a href="#categories">Categorías</a>
                <a href="#testimonials">Testimonios</a>
            </nav>
            <a href="#waitlist" class="cta-button">Unirse a Beta</a>
        </div>
    </header>

    <section class="hero">
        <div class="container">
            <h1 class="animate-fade-in">Tu idea vale dinero. <span class="highlight">Antes de construirla.</span></h1>
            <p class="animate-fade-in animate-delay-1">Fyrid es el marketplace donde las ideas se compran, venden y certifican como activos digitales.</p>
            
            <form class="waitlist-form animate-fade-in animate-delay-2" id="waitlistForm">
                <input type="email" placeholder="tu@email.com" required>
                <button type="submit">Reservar acceso →</button>
            </form>

            <div class="social-proof animate-fade-in animate-delay-3">
                <div class="proof-item">
                    <strong>142+</strong>
                    <span>Creadores en espera</span>
                </div>
                <div class="proof-item">
                    <strong>$25K+</strong>
                    <span>Valor en ideas</span>
                </div>
                <div class="proof-item">
                    <strong>8</strong>
                    <span>Empresas patrocinando</span>
                </div>
            </div>
        </div>
    </section>

    <section class="features" id="features">
        <div class="container">
            <div class="section-title">
                <h2>Transforma ideas en activos</h2>
                <p>Un ecosistema completo para crear, validar y monetizar tus ideas innovadoras</p>
            </div>

            <div class="features-grid">
                <div class="feature-card animate-fade-in">
                    <div class="feature-icon">🚀</div>
                    <h3>Marketplace en tiempo real</h3>
                    <p>Publica tu idea y observa cómo su valor aumenta con la interacción de la comunidad. Compra y vende ideas como activos digitales.</p>
                </div>

                <div class="feature-card animate-fade-in animate-delay-1">
                    <div class="feature-icon">🤖</div>
                    <h3>Validación con IA</h3>
                    <p>Nuestra inteligencia artificial clasifica y sugiere mejoras usando metodologías probadas como SCAMPER, Design Thinking y SMART Goals.</p>
                </div>

                <div class="feature-card animate-fade-in animate-delay-2">
                    <div class="feature-icon">💎</div>
                    <h3>Certificados Blockchain</h3>
                    <p>Cada idea adquirida recibe un certificado digital verificable e intransferible que garantiza tu propiedad intelectual.</p>
                </div>
            </div>
        </div>
    </section>

    <section class="demo" id="demo">
        <div class="container">
            <div class="section-title">
                <h2>Cómo funciona Fyrid</h2>
                <p>De la idea al activo digital en 3 simples pasos</p>
            </div>

            <div class="steps">
                <div class="step animate-fade-in">
                    <div class="step-number">1</div>
                    <h3>Crea tu idea</h3>
                    <p>Escribe tu concepto, selecciona categoría y deja que nuestra IA lo clasifique automáticamente en Fyrid Space, Industrial, Doméstico o Primitive.</p>
                    <div class="step-image">Mockup: Creación de idea</div>
                </div>

                <div class="step animate-fade-in animate-delay-1">
                    <div class="step-number">2</div>
                    <h3>Entra al marketplace</h3>
                    <p>Tu idea recibe una valoración inicial y comienza a ganar ⚡ Energy con cada interacción. Observa cómo su valor fluctúa en tiempo real.</p>
                    <div class="step-image">Mockup: Marketplace</div>
                </div>

                <div class="step animate-fade-in animate-delay-2">
                    <div class="step-number">3</div>
                    <h3>Trade y certifica</h3>
                    <p>Vende tu idea, recibe tokens y obtén tu certificado blockchain de propiedad intelectual. ¡Tu creatividad ahora es un activo tangible!</p>
                    <div class="step-image">Mockup: Certificado</div>
                </div>
            </div>
        </div>
    </section>

    <section class="categories" id="categories">
        <div class="container">
            <div class="section-title">
                <h2>Clasificación Fyrid</h2>
                <p>Cada idea se clasifica según su impacto y tiempo de ejecución</p>
            </div>

            <div class="categories-grid">
                <div class="category-card category-space animate-fade-in">
                    <div class="category-icon">🚀</div>
                    <h3>Fyrid Space</h3>
                    <p>Alto impacto + Ejecución rápida. Ideas disruptivas que pueden implementarse rápidamente con gran potencial de crecimiento.</p>
                </div>

                <div class="category-card category-industrial animate-fade-in animate-delay-1">
                    <div class="category-icon">⚡</div>
                    <h3>Fyrid Industrial</h3>
                    <p>Alto impacto + Ejecución larga. Proyectos de optimización de procesos y eficiencia para empresas y organizaciones.</p>
                </div>

                <div class="category-card category-domestico animate-fade-in animate-delay-2">
                    <div class="category-icon">🏠</div>
                    <h3>Fyrid Doméstico</h3>
                    <p>Bajo impacto + Ejecución rápida. Ideas para el hogar y la vida diaria que mejoran la calidad de vida personal.</p>
                </div>

                <div class="category-card category-primitive animate-fade-in animate-delay-3">
                    <div class="category-icon">🎯</div>
                    <h3>Fyrid Primitive</h3>
                    <p>Bajo impacto + Ejecución larga. Ideas disruptivas y de concepto inicial que pueden evolucionar con tiempo y recursos.</p>
                </div>
            </div>
        </div>
    </section>

    <section class="testimonials" id="testimonials">
        <div class="container">
            <div class="section-title">
                <h2 style="color: white;">Lo que dicen nuestros creadores</h2>
                <p style="color: rgba(255,255,255,0.9);">Historias reales de innovadores que ya están transformando ideas en valor</p>
            </div>

            <div class="testimonial-grid">
                <div class="testimonial-card animate-fade-in">
                    <p class="testimonial-text">Vendí mi idea de app de productividad por $1,200 antes de escribir una línea de código. La validación de la comunidad fue increíble y me ayudó a mejorar el concepto.</p>
                    <div class="testimonial-author">
                        <div class="author-avatar">ER</div>
                        <div class="author-info">
                            <h4>Elena Rodriguez</h4>
                            <p>Product Designer @ Google</p>
                        </div>
                    </div>
                </div>

                <div class="testimonial-card animate-fade-in animate-delay-1">
                    <p class="testimonial-text">Como developer indie, Fyrid me permitió monetizar mis side projects sin tener que construirlos todos. ¡Un game-changer para emprendedores solitarios!</p>
                    <div class="testimonial-author">
                        <div class="author-avatar">CM</div>
                        <div class="author-info">
                            <h4>Carlos Morales</h4>
                            <p>Indie Hacker & Creator</p>
                        </div>
                    </div>
                </div>

                <div class="testimonial-card animate-fade-in animate-delay-2">
                    <p class="testimonial-text">Nuestra empresa patrocinó un reto de innovación y recibimos 50+ ideas increíbles en una semana. El ROI fue excepcional y encontramos talento increíble.</p>
                    <div class="testimonial-author">
                        <div class="author-avatar">EI</div>
                        <div class="author-info">
                            <h4>EcoInnova Team</h4>
                            <p>Startup de Sostenibilidad</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="cta-section" id="waitlist">
        <div class="container">
            <h2 class="animate-fade-in">¿Listo para monetizar tus ideas?</h2>
            <p class="animate-fade-in animate-delay-1">Únete a la beta cerrada y sé de los primeros en transformar creatividad en capital.</p>

            <form class="cta-form animate-fade-in animate-delay-2" id="finalWaitlistForm">
                <input type="email" placeholder="tu@email.com" required>
                <button type="submit">Acceder a Beta</button>
            </form>

            <div class="cta-benefits animate-fade-in animate-delay-3">
                <div class="cta-benefit">
                    <strong>Early Access</strong>
                    <span>Acceso prioritario</span>
                </div>
                <div class="cta-benefit">
                    <strong>100 Fyrid</strong>
                    <span>Tokens de bienvenida</span>
                </div>
                <div class="cta-benefit">
                    <strong>Badge</strong>
                    <span>Perfil exclusivo</span>
                </div>
            </div>
        </div>
    </section>

    <footer>
        <div class="container">
            <div class="footer-grid">
                <div class="footer-column">
                    <a href="#" class="logo" style="color: white; margin-bottom: 1.5rem; display: inline-flex; align-items: center; gap: 0.75rem; text-decoration: none;">
                        <div class="logo-icon">F</div>
                        <span>Fyrid</span>
                    </a>
                    <p style="color: #cbd5e1; margin-top: 1rem; line-height: 1.7;">
                        El marketplace de ideas innovadoras. Transformando creatividad en activos digitales desde 2024.
                    </p>
                </div>

                <div class="footer-column">
                    <h3>Enlaces</h3>
                    <ul class="footer-links">
                        <li><a href="#features">Características</a></li>
                        <li><a href="#demo">Cómo funciona</a></li>
                        <li><a href="#categories">Categorías</a></li>
                        <li><a href="#testimonials">Testimonios</a></li>
                    </ul>
                </div>

                <div class="footer-column">
                    <h3>Legal</h3>
                    <ul class="footer-links">
                        <li><a href="#">Términos de servicio</a></li>
                        <li><a href="#">Política de privacidad</a></li>
                        <li><a href="#">Propiedad intelectual</a></li>
                    </ul>
                </div>

                <div class="footer-column">
                    <h3>Contacto</h3>
                    <ul class="footer-links">
                        <li><a href="mailto:hello@fyrid.app">hello@fyrid.app</a></li>
                        <li><a href="#">Twitter</a></li>
                        <li><a href="#">LinkedIn</a></li>
                    </ul>
                </div>
            </div>

            <div class="copyright">
                <p>&copy; 2024 Fyrid. Todos los derechos reservados.</p>
            </div>
        </div>
    </footer>

    <script>
        document.getElementById('waitlistForm').addEventListener('submit', function(e) {
            e.preventDefault();
            const email = this.querySelector('input[type="email"]').value;
            alert('¡Excelente! ' + email + ' ha sido añadido a la lista de espera. Te contactaremos pronto.');
            this.reset();
        });

        document.getElementById('finalWaitlistForm').addEventListener('submit', function(e) {
            e.preventDefault();
            const email = this.querySelector('input[type="email"]').value;
            alert('¡Excelente! ' + email + ' ha sido añadido a la lista de espera. Te contactaremos pronto.');
            this.reset();
        });

        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const targetId = this.getAttribute('href');
                if (targetId === '#') return;
                const target = document.querySelector(targetId);
                if (target) {
                    window.scrollTo({
                        top: target.offsetTop - 80,
                        behavior: 'smooth'
                    });
                }
            });
        });

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('animate-fade-in');
                }
            });
        }, { threshold: 0.1 });

        document.querySelectorAll('.feature-card, .step, .category-card, .testimonial-card').forEach(el => {
            observer.observe(el);
        });

        window.addEventListener('scroll', function() {
            const header = document.querySelector('header');
            if (window.scrollY > 50) {
                header.style.background = 'rgba(255, 255, 255, 0.98)';
                header.style.boxShadow = '0 10px 30px rgba(0, 0, 0, 0.1)';
            } else {
                header.style.background = 'rgba(255, 255, 255, 0.95)';
                header.style.boxShadow = 'var(--shadow-sm)';
            }
        });
    </script>
</body>
</html>
